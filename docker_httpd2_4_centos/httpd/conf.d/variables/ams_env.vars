# Most of the sysconfig are configured at /etc/sysconfig/httpd file

## Author
#Define AUTHOR_DEFAULT_HOSTNAME author.localhost
#Define AUTHOR_DOCROOT /var/www/html/author
#Define DISP_ID <value need to be mentioned>

## Publish
#Define PUBLISH_DOCROOT /var/www/html/publish
#Define PUBLISH_DEFAULT_HOSTNAME publish.localhost
#Define LIVECYCLE_FORCE_SSL 1

##Asset Donwload defaults to deny but can be allowed by setting below to allow
#Define ASSET_DOWNLOAD_RULE deny
